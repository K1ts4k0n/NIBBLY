"use client";
import Link from "next/link";
import { Minus, Plus, ShoppingBag, Trash2, X } from "lucide-react";
import { AnimatePresence, motion } from "framer-motion";
import { useCart } from "@/components/providers/cart-provider";
import { ProductArt } from "@/components/ui/product-art";
import { useLanguage } from "@/components/providers/language-provider";
export function CartDrawer() { const {items, isOpen, setOpen, change, remove, total, toast} = useCart(); const {t}=useLanguage(); return <><AnimatePresence>{toast&&<motion.div initial={{opacity:0,y:-18,scale:.9}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:-18}} className="fixed left-1/2 top-24 z-[80] -translate-x-1/2 rounded-full bg-[#354B2D] px-5 py-3 text-sm font-bold text-white shadow-xl">{t("added")}</motion.div>}</AnimatePresence><AnimatePresence>{isOpen && <><motion.button aria-label="Close cart" onClick={() => setOpen(false)} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-50 cursor-default bg-[#354B2D]/25 backdrop-blur-sm"/><motion.aside initial={{x:"100%"}} animate={{x:0}} exit={{x:"100%"}} transition={{type:"spring", damping:28, stiffness:300}} className="fixed right-0 top-0 z-[60] flex h-dvh w-full max-w-md flex-col bg-[#FFF8E8] p-5 shadow-2xl"><div className="flex items-center justify-between border-b border-[#e5d5b5] pb-5"><h2 className="font-display text-3xl text-[#354B2D]">{t("cart")}</h2><button aria-label="Close cart" onClick={() => setOpen(false)} className="rounded-full p-2 hover:bg-[#FFF2B4]"><X/></button></div>{items.length ? <div className="flex min-h-0 flex-1 flex-col"><div className="flex-1 overflow-y-auto py-5">{items.map(line => <div key={line.id} className="mb-4 flex gap-3"><ProductArt variant={line.art} image={line.image} alt={line.name} className="size-20 shrink-0 rounded-2xl"/><div className="min-w-0 flex-1"><p className="font-display text-lg text-[#354B2D]">{line.name}</p><p className="text-sm text-[#8B5E3C]">฿{line.price}</p><div className="mt-2 flex items-center justify-between"><div className="flex items-center rounded-full border border-[#e5d5b5] bg-white"><button aria-label="Reduce quantity" onClick={() => change(line.id, line.quantity - 1)} className="p-1.5"><Minus size={13}/></button><span className="w-6 text-center text-xs font-bold">{line.quantity}</span><button aria-label="Increase quantity" onClick={() => change(line.id, line.quantity + 1)} className="p-1.5"><Plus size={13}/></button></div><button aria-label="Remove item" onClick={() => remove(line.id)} className="p-1 text-stone-400 hover:text-[#FF718D]"><Trash2 size={16}/></button></div></div></div>)}</div><div className="border-t border-[#e5d5b5] pt-4"><div className="mb-5 flex justify-between font-display text-2xl text-[#354B2D]"><span>Total</span><span>฿{total}</span></div><Link href="/shop" onClick={() => setOpen(false)} className="mb-2 block rounded-full border border-[#354B2D] py-3 text-center text-sm font-bold text-[#354B2D]">{t("viewCart")}</Link><Link href="/checkout" onClick={() => setOpen(false)} className="block w-full rounded-full bg-[#FF718D] py-3 text-center text-sm font-bold text-white transition hover:bg-[#A678FF]">{t("checkout")}</Link></div></div> : <div className="grid flex-1 place-items-center text-center"><div><div className="mx-auto grid size-16 place-items-center rounded-full bg-[#FFF2B4] text-[#FF9F43]"><ShoppingBag/></div><h3 className="mt-4 font-display text-2xl text-[#354B2D]">{t("cart")}</h3><p className="mt-2 text-sm text-stone-500">{t("emptySub")}</p><Link onClick={() => setOpen(false)} href="/shop" className="mt-5 inline-block rounded-full bg-[#354B2D] px-5 py-3 text-sm font-bold text-white">{t("browse")}</Link></div></div>}</motion.aside></>}</AnimatePresence></> }
